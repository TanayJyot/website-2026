---
tags: 
aliases: []
---
# vectorization
##
- Basically when you use NumPy, it uses the vector of $w$ instead of the value to conduct the calculation. 
- NumPy processes all values in parallel using [[parallel processing]] that conducts all the calculations in the same time step. 
- 
Len allows us to find the length of a string. ( The number of characters)
```python
a = len("TanayJyot")
```
Outputs 9 

Spaces are also counted

## [[9.6, 9.7, 9.8, 9.9]]
### How `len` works
- Each of the [[collection data type]]s have a special attribute that stores their size automatically so when we use `len` we just access this [[attribute]]. 
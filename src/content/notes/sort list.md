- Use .sort to to change the list itself in an ordered manner. 
- Unlike [[sorted()]] where the original list is unchanged. 
```python
List1 = ["c", "a", "b"]
List2 = List1.sort()
print(List1, List2) 
# Outputs ['a', 'b', 'c'] None
```
- Returns No value/ ‘None’
- Changes the original list 
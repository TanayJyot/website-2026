---
tags: 
aliases:
---
# regularization
##
- Penalize the algorithm for creating an algorithm that is too complicated and telling it to use something less complicated to map the same data
- This is used with [[overfitting]] because the design of your algorithm works so instead of changing it just make it less complicated.
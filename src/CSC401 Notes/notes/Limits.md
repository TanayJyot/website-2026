## 1.1 The idea of limit
- The limit allows us to find which value a particular function is approaching as it acts. 
- This allows us to get very close to places where the value of the function is actually undefined. 
- The function must approach one number for their to be a limit. 
- ![[Pasted image 20230926021724.png]]
- Going from the left coming to the right does not exist. 
![[Pasted image 20231007213040.png]]

___

[[limits that do not exist]]


- The value keeps fluctuating between -1 and 1 so it never stops at one so it isn’t a limit. 
- The floor approaches a value when the floor tries to get to that value but never reaches that value. 

[[Epsilon Delta Proofs of a limit]]

## Limits at Infinity
![[Limits at Infinity]]

## Proving Limits
You take $\epsilon$ and then try to find $\delta$ on the basis of that. Then use $\delta$ to prove $\epsilon$. 

